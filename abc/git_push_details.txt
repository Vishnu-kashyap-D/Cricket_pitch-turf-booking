# Git Push Details for Cricket Pitch Booking System

## Remote Repository
https://github.com/Vishnu-kashyap-D/DBMS_mini_pj.git

## Branch Used
master

## Commands Used

# Initialize git repository
```
git init
```

# Add all files to staging
```
git add .
```

# Commit the files
```
git commit -m "Initial commit: Cricket Pitch Booking System project"
```

# Add remote origin
```
git remote add origin https://github.com/Vishnu-kashyap-D/DBMS_mini_pj.git
```

# Push to GitHub (master branch)
```
git push -u origin master
```

## Notes
- If you make further changes, use:
  ```
  git add .
  git commit -m "Your message"
  git push
  ```
- If you encounter ownership or permission issues, you may need to mark the directory as safe:
  ```
  git config --global --add safe.directory "V:/vishnu/notes pdf/4th SEM/DBMS/Project/abc"
  ``` 